{
  "name": "buyback-burn-bot",
  "version": "1.0.0",
  "description": "USDG -> TOKEN buyback and burn bot for Robinhood Chain (EVM)",
  "type": "module",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "node dist/index.js",
    "dev": "tsx src/index.ts",
    "run-once": "tsx src/index.ts --once"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "viem": "^2.21.0"
  },
  "devDependencies": {
    "@types/node": "^20.14.0",
    "tsx": "^4.16.0",
    "typescript": "^5.5.0"
  }
}
